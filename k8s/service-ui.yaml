apiVersion: v1
kind: Service
metadata:
    name: fpfl-ui-service
    namespace: fpfl
    labels:
        app: fpfl-ui
spec:
    type: ClusterIP
    selector:
        app: fpfl-ui
    ports:
        - port: 80
          targetPort: 80
