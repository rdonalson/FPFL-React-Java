version: "3.9"

services:
    api:
        build:
            context: ..
            dockerfile: ../Dockerfile
        container_name: fpflapijdk
        environment:
            SPRING_DATASOURCE_URL: jdbc:postgresql://host.docker.internal:5432/FPFL-V2
            SPRING_DATASOURCE_USERNAME: postgres
            SPRING_DATASOURCE_PASSWORD: xbt20o19A
            SPRING_JPA_HIBERNATE_DDL_AUTO: update
            SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA: fpfl

            # Must match your Dockerfile + Kubernetes containerPort
            SERVER_PORT: 8080
        ports:
            - "8080:8080"
